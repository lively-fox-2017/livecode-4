<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
  </head>
  <body>
    <div style="display: inline-block">
      <h2><%= title %></h2>
      <%= err ? err : "" %>
    </div>
    <form action="<%= aksi %>" method="post">
      <table>
        <tr>
          <td>Name</td>
          <td>:</td>
          <td><input type="text" name="name" value="<%= item ? item.name : "" %>"></td>
        </tr>
        <tr>
          <td>Brand</td>
          <td>:</td>
          <td><input type="text" name="brand" value="<%= item ? item.brand : "" %>"></td>
        </tr>
        <tr>
          <td>Code Item</td>
          <td>:</td>
          <td><input type="text" name="codeitem" value="<%= item ? item.codeitem : "" %>"></td>
        </tr>
        <tr>
          <td colspan="3"><button type="submit" name="button">Submit</button></td>
        </tr>
      </table>
    </form>
  </body>
</html>
